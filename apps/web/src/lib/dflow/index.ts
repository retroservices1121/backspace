// Re-export surface for the Dflow integration. Phase 1 ships the
// config only; Phase 3 will add the client (quote, swap, sign,
// submit) and Phase 2 will add the token catalog helpers.
export * from './config';
