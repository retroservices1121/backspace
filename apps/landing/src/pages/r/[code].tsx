// Referral URL — renders the same landing page as `/`. The dynamic
// `code` segment is read by the page component via useRouter() and
// forwarded into the POST /api/waitlist body so the inviter gets
// credited. OG / Twitter Card tags from _app.tsx render here too.
export { default } from '../index';
